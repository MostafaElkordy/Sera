name: Sera
description: تطبيق ذكي للطوارئ والإسعافات الأولية
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  go_router: ^12.1.3
  intl: ^0.20.2
  # audioplayers removed to restore stable build
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
