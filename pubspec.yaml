name: sera
description: تطبيق ذكي للطوارئ والإسعافات الأولية
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  # go_router: ^17.0.0  # Removed - using Navigator
  intl: ^0.20.2
  shared_preferences: ^2.2.2
  geolocator: ^11.0.0
  audioplayers: ^6.0.0
  sqflite: ^2.3.2
  path: ^1.9.0
  path_provider: ^2.1.2
  connectivity_plus: ^5.0.2
  url_launcher: ^6.3.2
  flutter_contacts: ^1.1.9+2
  # camera: ^0.11.3  # Removed - unused
  # record: ^6.1.2  # Removed - unused
  share_plus: ^12.0.1
  permission_handler: ^12.0.1
  vibration: ^3.1.5
  fluttertoast: ^9.0.0
  image_picker: ^1.2.1

  # ===== AI Integration Packages =====
  google_generative_ai: ^0.4.6 # Updated to support latest Gemini models
  camera: ^0.10.5+5 # Camera capture
  speech_to_text: ^7.0.0 # Voice recognition
  flutter_tts: ^4.0.2 # Text to speech
  image: ^4.1.3 # Image processing
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
